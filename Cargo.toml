[package]
name = "windows_test"
version = "0.1.0"
edition = "2024"

[dependencies]
eframe = "0.25"  # 提供跨平台窗口支持
egui = "0.25"     # GUI 核心库
winapi = { version = "0.3", features = ["winuser", "libloaderapi", "shellapi"] }
include_dir = "0.7"  # 用于嵌入字体文件